<svg xmlns="http://www.w3.org/2000/svg" data-theme="dark" viewBox="0 0 1150 402" role="img" aria-label="逆命仙途 — Java-driven text cultivation roguelike (architecture diagram)"><style>
    /* ==========================================================
       THEME VARIABLES �?switch by toggling [data-theme] on <html>
       ========================================================== */
    :root,
    [data-theme="dark"] {
      --bg: #020617;
      --grid: #1e293b;
      --text: #ffffff;
      --text-muted: #94a3b8;
      --text-dim: #475569;
      /* UI chrome (menu hints, footer) needs more contrast than SVG .t-dim accents */
      --text-faint: #7d8da1;

      --panel: rgba(15, 23, 42, 0.5);
      --panel-border: #1e293b;
      --lane-fill: rgba(15, 23, 42, 0.22);
      --lane-stroke: #334155;

      --arrow: #64748b;
      --arrow-emphasis: #34d399;

      /* Opaque mask color used behind semi-transparent component fills
         so the arrows drawn underneath are properly hidden. */
      --mask: #0f172a;

      --frontend-fill:    rgba(8, 51, 68, 0.4);
      --frontend-stroke:  #22d3ee;
      --backend-fill:     rgba(6, 78, 59, 0.4);
      --backend-stroke:   #34d399;
      --database-fill:    rgba(76, 29, 149, 0.4);
      --database-stroke:  #a78bfa;
      --cloud-fill:       rgba(120, 53, 15, 0.3);
      --cloud-stroke:     #fbbf24;
      --security-fill:    rgba(136, 19, 55, 0.4);
      --security-stroke:  #fb7185;
      --messagebus-fill:  rgba(251, 146, 60, 0.3);
      --messagebus-stroke:#fb923c;
      --external-fill:    rgba(30, 41, 59, 0.5);
      --external-stroke:  #94a3b8;

      --toolbar-bg:       rgba(15, 23, 42, 0.8);
      --toolbar-border:   #334155;
      --toolbar-text:     #e2e8f0;
      --toolbar-hover:    rgba(15, 23, 42, 0.95);
      --toolbar-menu-bg:  #0f172a;
    }

    [data-theme="light"] {
      --bg: #f8fafc;
      --grid: #e2e8f0;
      --text: #0f172a;
      --text-muted: #64748b;
      --text-dim: #94a3b8;
      --text-faint: #64748b;

      --panel: #ffffff;
      --panel-border: #e2e8f0;
      --lane-fill: rgba(248, 250, 252, 0.65);
      --lane-stroke: #cbd5e1;

      --arrow: #94a3b8;
      --arrow-emphasis: #059669;

      --mask: #ffffff;

      --frontend-fill:    rgba(34, 211, 238, 0.15);
      --frontend-stroke:  #0891b2;
      --backend-fill:     rgba(52, 211, 153, 0.18);
      --backend-stroke:   #059669;
      --database-fill:    rgba(167, 139, 250, 0.2);
      --database-stroke:  #7c3aed;
      --cloud-fill:       rgba(251, 191, 36, 0.18);
      --cloud-stroke:     #d97706;
      --security-fill:    rgba(251, 113, 133, 0.15);
      --security-stroke:  #e11d48;
      --messagebus-fill:  rgba(251, 146, 60, 0.15);
      --messagebus-stroke:#ea580c;
      --external-fill:    rgba(148, 163, 184, 0.18);
      --external-stroke:  #64748b;

      --toolbar-bg:       rgba(255, 255, 255, 0.92);
      --toolbar-border:   #cbd5e1;
      --toolbar-text:     #334155;
      --toolbar-hover:    #ffffff;
      --toolbar-menu-bg:  #ffffff;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: 'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, Consolas, 'DejaVu Sans Mono', 'Liberation Mono', 'Noto Sans Mono CJK SC', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', monospace;
      background: var(--bg);
      min-height: 100vh;
      padding: 2rem;
      color: var(--text);
      transition: background 0.2s ease, color 0.2s ease;
    }

    .container { max-width: 1200px; margin: 0 auto; }

    .header { margin-bottom: 2rem; }

    .header-row {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-bottom: 0.5rem;
    }

    .pulse-dot {
      width: 12px;
      height: 12px;
      background: var(--frontend-stroke);
      border-radius: 50%;
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.5; }
    }

    h1 {
      font-size: 1.5rem;
      font-weight: 700;
      letter-spacing: -0.025em;
    }

    .subtitle {
      color: var(--text-muted);
      font-size: 0.875rem;
      margin-left: 1.75rem;
    }

    .diagram-container {
      background: var(--panel);
      border-radius: 1rem;
      border: 1px solid var(--panel-border);
      padding: 1.5rem;
      overflow-x: auto;
    }

    svg {
      width: 100%;
      min-width: min(900px, 100%);
      display: block;
    }

    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 1rem;
      margin-top: 2rem;
    }

    .card {
      background: var(--panel);
      border-radius: 0.75rem;
      border: 1px solid var(--panel-border);
      padding: 1.25rem;
    }

    .card-header {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      margin-bottom: 0.75rem;
    }

    .card-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
    }

    .card-dot.cyan     { background: var(--frontend-stroke); }
    .card-dot.emerald  { background: var(--backend-stroke); }
    .card-dot.violet   { background: var(--database-stroke); }
    .card-dot.amber    { background: var(--cloud-stroke); }
    .card-dot.rose     { background: var(--security-stroke); }
    .card-dot.orange   { background: var(--messagebus-stroke); }
    .card-dot.slate    { background: var(--external-stroke); }

    .card h3 { font-size: 0.875rem; font-weight: 600; color: var(--text); }

    .card ul {
      list-style: none;
      color: var(--text-muted);
      font-size: 0.75rem;
    }

    .card li { margin-bottom: 0.375rem; }

    .footer {
      text-align: center;
      margin-top: 1.5rem;
      color: var(--text-faint);
      font-size: 0.75rem;
    }

    /* ==========================================================
       Print stylesheet �?hide interactive controls, force light,
       drop the grid so the diagram doesn't waste ink, use landscape
       so wide diagrams fit, pack summary cards into 2 columns to
       avoid an orphan card on a trailing page.
       ========================================================== */
    @page { size: landscape; margin: 1.5cm; }

    @media print {
      /* Force the FULL light palette �?including component fills/strokes and
         lane/arrow colors �?so printing from dark theme doesn't put neon
         strokes and translucent dark fills on white paper. */
      :root, [data-theme="dark"], [data-theme="light"] {
        --bg: #ffffff;
        --grid: transparent;
        --panel: #ffffff;
        --panel-border: #e2e8f0;
        --text: #0f172a;
        --text-muted: #475569;
        --text-dim: #94a3b8;
        --text-faint: #64748b;
        --mask: #ffffff;
        --lane-fill: rgba(248, 250, 252, 0.65);
        --lane-stroke: #cbd5e1;
        --arrow: #94a3b8;
        --arrow-emphasis: #059669;
        --frontend-fill:    rgba(34, 211, 238, 0.15);
        --frontend-stroke:  #0891b2;
        --backend-fill:     rgba(52, 211, 153, 0.18);
        --backend-stroke:   #059669;
        --database-fill:    rgba(167, 139, 250, 0.2);
        --database-stroke:  #7c3aed;
        --cloud-fill:       rgba(251, 191, 36, 0.18);
        --cloud-stroke:     #d97706;
        --security-fill:    rgba(251, 113, 133, 0.15);
        --security-stroke:  #e11d48;
        --messagebus-fill:  rgba(251, 146, 60, 0.15);
        --messagebus-stroke:#ea580c;
        --external-fill:    rgba(148, 163, 184, 0.18);
        --external-stroke:  #64748b;
      }
      body { background: #ffffff !important; padding: 0; }
      .container { max-width: none; }
      .toolbar, .archify-toast, .no-print { display: none !important; }
      .diagram-container, .card { box-shadow: none; border-color: #e2e8f0; }
      .cards { grid-template-columns: 1fr 1fr; }
      /* Ensure elements break sensibly across pages */
      .card { break-inside: avoid; page-break-inside: avoid; }
      .diagram-container { break-inside: avoid; page-break-inside: avoid; }
      h1, .subtitle { color: #0f172a; }
    }

    /* ==========================================================
       TOOLBAR (theme toggle + export menu)
       ========================================================== */
    .toolbar {
      position: fixed;
      top: 1rem;
      right: 1rem;
      display: flex;
      gap: 0.5rem;
      z-index: 100;
    }
    .toolbar button {
      background: var(--toolbar-bg);
      color: var(--toolbar-text);
      border: 1px solid var(--toolbar-border);
      padding: 0.5rem 0.875rem;
      border-radius: 0.5rem;
      font-family: inherit;
      font-size: 0.75rem;
      font-weight: 500;
      cursor: pointer;
      backdrop-filter: blur(8px);
      transition: background 0.15s, border-color 0.15s;
      display: inline-flex;
      align-items: center;
      gap: 0.375rem;
    }
    .toolbar button:hover {
      background: var(--toolbar-hover);
      border-color: var(--arrow);
    }
    .toolbar button:focus-visible {
      outline: 2px solid var(--frontend-stroke);
      outline-offset: 2px;
    }
    .toolbar .export-wrap { position: relative; }
    .toolbar .export-menu {
      position: absolute;
      top: calc(100% + 0.375rem);
      right: 0;
      background: var(--toolbar-menu-bg);
      border: 1px solid var(--toolbar-border);
      border-radius: 0.5rem;
      padding: 0.375rem;
      min-width: 160px;
      display: none;
      flex-direction: column;
      box-shadow: 0 4px 20px rgba(0,0,0,0.25);
    }
    .toolbar .export-menu.open { display: flex; }
    .toolbar .export-menu button {
      background: transparent;
      border: 0;
      justify-content: space-between;
      width: 100%;
      text-align: left;
      padding: 0.5rem 0.75rem;
    }
    .toolbar .export-menu button:hover {
      background: var(--toolbar-hover);
    }
    .toolbar .export-menu .hint {
      color: var(--text-faint);
      font-size: 0.625rem;
    }
    .toolbar .export-menu hr {
      border: 0;
      border-top: 1px solid var(--toolbar-border);
      margin: 0.25rem 0.375rem;
      opacity: 0.5;
    }

    /* GIF/WebM export settings dialog */
    .archify-dialog-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.5);
      display: none;
      align-items: center;
      justify-content: center;
      z-index: 10000;
    }
    .archify-dialog-overlay.open { display: flex; }
    .archify-dialog {
      background: var(--toolbar-menu-bg);
      border: 1px solid var(--toolbar-border);
      border-radius: 0.75rem;
      padding: 1.5rem;
      min-width: 320px;
      max-width: 400px;
      box-shadow: 0 8px 32px rgba(0,0,0,0.4);
    }
    .archify-dialog h3 {
      margin: 0 0 1rem;
      font-size: 0.9rem;
      font-weight: 600;
      color: var(--text-primary);
    }
    .archify-dialog label {
      display: block;
      font-size: 0.7rem;
      color: var(--text-muted);
      margin-bottom: 0.25rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }
    .archify-dialog .form-row {
      margin-bottom: 1rem;
    }
    .archify-dialog select {
      width: 100%;
      padding: 0.5rem 0.625rem;
      background: var(--bg);
      color: var(--text-primary);
      border: 1px solid var(--toolbar-border);
      border-radius: 0.375rem;
      font-size: 0.8rem;
      font-family: inherit;
      cursor: pointer;
    }
    .archify-dialog select:focus {
      outline: none;
      border-color: var(--accent);
    }
    .archify-dialog .dialog-actions {
      display: flex;
      gap: 0.5rem;
      justify-content: flex-end;
      margin-top: 1.25rem;
    }
    .archify-dialog .btn {
      padding: 0.5rem 1rem;
      border: 1px solid var(--toolbar-border);
      border-radius: 0.375rem;
      background: transparent;
      color: var(--text-primary);
      font-size: 0.8rem;
      cursor: pointer;
      font-family: inherit;
    }
    .archify-dialog .btn:hover {
      background: var(--toolbar-hover);
    }
    .archify-dialog .btn-primary {
      background: var(--toolbar-hover);
      color: var(--text-primary);
      border-color: var(--accent);
      font-weight: 600;
    }
    .archify-dialog .btn-primary:hover {
      background: var(--toolbar-border);
    }
    .archify-dialog .form-hint {
      font-size: 0.65rem;
      color: var(--text-faint);
      margin-top: 0.25rem;
    }

    /* Toast �?brief feedback for actions like "copied" */
    .archify-toast {
      position: fixed;
      top: 1rem;
      left: 50%;
      transform: translateX(-50%) translateY(-8px);
      background: var(--toolbar-menu-bg);
      color: var(--text);
      border: 1px solid var(--toolbar-border);
      padding: 0.5rem 1rem;
      border-radius: 0.5rem;
      font-family: inherit;
      font-size: 0.75rem;
      opacity: 0;
      transition: opacity 0.2s, transform 0.2s;
      z-index: 200;
      pointer-events: none;
      box-shadow: 0 4px 20px rgba(0,0,0,0.25);
    }
    .archify-toast.show {
      opacity: 1;
      transform: translateX(-50%) translateY(0);
    }

    @media (max-width: 720px) {
      body { padding: 1rem; }
      .toolbar {
        position: static;
        justify-content: flex-end;
        margin-bottom: 1rem;
      }
      .header { margin-bottom: 1.25rem; }
      .header-row {
        gap: 0.75rem;
        align-items: flex-start;
      }
      h1 {
        font-size: 1.25rem;
        line-height: 1.25;
      }
      .subtitle {
        margin-left: 0;
        font-size: 0.8125rem;
        line-height: 1.55;
      }
      .diagram-container {
        padding: 0.75rem;
        border-radius: 0.75rem;
      }
      .cards {
        grid-template-columns: 1fr;
        margin-top: 1rem;
      }
    }

    /* ==========================================================
       SVG SEMANTIC CLASSES �?use these instead of inline fill/stroke
       ========================================================== */
    .c-grid       { stroke: var(--grid); fill: none; }
    .c-mask       { fill: var(--mask); stroke: none; }

    .c-frontend   { fill: var(--frontend-fill);   stroke: var(--frontend-stroke); }
    .c-backend    { fill: var(--backend-fill);    stroke: var(--backend-stroke); }
    .c-database   { fill: var(--database-fill);   stroke: var(--database-stroke); }
    .c-cloud      { fill: var(--cloud-fill);      stroke: var(--cloud-stroke); }
    .c-security   { fill: var(--security-fill);   stroke: var(--security-stroke); }
    .c-messagebus { fill: var(--messagebus-fill); stroke: var(--messagebus-stroke); }
    .c-external   { fill: var(--external-fill);   stroke: var(--external-stroke); }

    /* Text helpers */
    .t-primary { fill: var(--text); }
    .t-muted   { fill: var(--text-muted); }
    .t-dim     { fill: var(--text-dim); }
    .t-frontend   { fill: var(--frontend-stroke); }
    .t-backend    { fill: var(--backend-stroke); }
    .t-database   { fill: var(--database-stroke); }
    .t-cloud      { fill: var(--cloud-stroke); }
    .t-security   { fill: var(--security-stroke); }
    .t-messagebus { fill: var(--messagebus-stroke); }
    .t-external   { fill: var(--external-stroke); }

    /* Arrows */
    .a-default   { stroke: var(--arrow); fill: none; }
    .a-emphasis  { stroke: var(--arrow-emphasis); fill: none; }
    .a-security  { stroke: var(--security-stroke); fill: none; stroke-dasharray: 5,5; }
    .a-dashed    { stroke: var(--database-stroke); fill: none; stroke-dasharray: 4,4; }

    /* ==========================================================
       FLOWING ANIMATION EFFECTS
       ========================================================== */
    @keyframes flowDash {
      to {
        stroke-dashoffset: -20;
      }
    }

    @keyframes flowPulse {
      0%, 100% {
        opacity: 0.6;
      }
      50% {
        opacity: 1;
      }
    }

    /* Flowing animation for emphasis paths (main data flow) */
    .a-emphasis {
      stroke-dasharray: 8 4;
      animation: flowDash 0.8s linear infinite, flowPulse 2s ease-in-out infinite;
      filter: drop-shadow(0 0 3px var(--arrow-emphasis));
    }

    /* Slower flowing for default paths */
    .a-default {
      stroke-dasharray: 6 6;
      animation: flowDash 1.2s linear infinite;
      filter: drop-shadow(0 0 2px var(--arrow));
    }

    /* Dashed async paths */
    .a-dashed {
      stroke-dasharray: 4 4;
      animation: flowDash 1s linear infinite;
    }

    /* Security paths */
    .a-security {
      stroke-dasharray: 5 5;
      animation: flowDash 1s linear infinite;
    }

    /* Arrowhead markers reference these */
    .m-default   { fill: var(--arrow); }
    .m-emphasis  { fill: var(--arrow-emphasis); }
    .m-security  { fill: var(--security-stroke); }
    .m-dashed    { fill: var(--database-stroke); }

    /* Dashed boundary variants */
    .c-security-group { fill: transparent; stroke: var(--security-stroke); stroke-dasharray: 4,4; }
    .c-region         { fill: rgba(251, 191, 36, 0.05); stroke: var(--cloud-stroke); stroke-dasharray: 8,4; }
    .c-lane           { fill: var(--lane-fill); stroke: var(--lane-stroke); stroke-dasharray: 6,6; }
  </style>
        <!-- Definitions -->
        <defs>
          <marker id="arrowhead" markerWidth="10" markerHeight="7" refX="9" refY="3.5" orient="auto">
            <polygon points="0 0, 10 3.5, 0 7" class="m-default" />
          </marker>
          <marker id="arrowhead-emphasis" markerWidth="10" markerHeight="7" refX="9" refY="3.5" orient="auto">
            <polygon points="0 0, 10 3.5, 0 7" class="m-emphasis" />
          </marker>
          <marker id="arrowhead-security" markerWidth="10" markerHeight="7" refX="9" refY="3.5" orient="auto">
            <polygon points="0 0, 10 3.5, 0 7" class="m-security" />
          </marker>
          <marker id="arrowhead-dashed" markerWidth="10" markerHeight="7" refX="9" refY="3.5" orient="auto">
            <polygon points="0 0, 10 3.5, 0 7" class="m-dashed" />
          </marker>
          <pattern id="grid" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M 40 0 L 0 0 0 40" class="c-grid" stroke-width="0.5"/>
          </pattern>
        </defs>

        <!-- Background Grid -->
        <rect width="100%" height="100%" fill="url(#grid)" />

        <!-- Boundaries (behind everything) -->
        <rect x="200" y="220" width="700" height="114" rx="12" class="c-region" stroke-width="1"/>
        <text x="208" y="238" class="t-cloud" font-size="9" font-weight="600">逆命仙途 application</text>

        <!-- Connection paths (before components for correct z-order) -->
        <path d="M 160 282 L 230 282" class="a-emphasis" stroke-width="1.8" marker-end="url(#arrowhead-emphasis)"/>
        <path d="M 390 282 L 470 282" class="a-emphasis" stroke-width="1.8" marker-end="url(#arrowhead-emphasis)"/>
        <path d="M 630 282 L 710 282" class="a-default" stroke-width="1.5" marker-end="url(#arrowhead)"/>
        <path d="M 870 282 L 950 282" class="a-default" stroke-width="1.5" marker-end="url(#arrowhead)"/>

        <!-- Animated flowing dots -->
        <circle r="3" fill="var(--arrow-emphasis)" opacity="0.9">
          <animateMotion dur="2s" repeatCount="indefinite" path="M 160 282 L 230 282"/>
        </circle>
        <circle r="3" fill="var(--arrow-emphasis)" opacity="0.9">
          <animateMotion dur="2.5s" repeatCount="indefinite" path="M 390 282 L 470 282"/>
        </circle>
        <circle r="2.5" fill="var(--arrow)" opacity="0.8">
          <animateMotion dur="3.1s" repeatCount="indefinite" path="M 630 282 L 710 282"/>
        </circle>
        <circle r="2.5" fill="var(--arrow)" opacity="0.8">
          <animateMotion dur="3.4s" repeatCount="indefinite" path="M 870 282 L 950 282"/>
        </circle>

        <!-- Components -->
        <rect x="20" y="250" width="140" height="64" rx="6" class="c-mask"/>
        <rect x="20" y="250" width="140" height="64" rx="6" class="c-external" stroke-width="1.5"/>
        <text x="90" y="280" class="t-primary" font-size="11" font-weight="600" text-anchor="middle">Player</text>
        <text x="90" y="296" class="t-muted" font-size="9" text-anchor="middle">Browser</text>

        <rect x="230" y="250" width="160" height="64" rx="6" class="c-mask"/>
        <rect x="230" y="250" width="160" height="64" rx="6" class="c-frontend" stroke-width="1.5"/>
        <text x="310" y="280" class="t-primary" font-size="11" font-weight="600" text-anchor="middle">React UI</text>
        <text x="310" y="296" class="t-muted" font-size="9" text-anchor="middle">Vite + TypeScript</text>

        <rect x="470" y="250" width="160" height="64" rx="6" class="c-mask"/>
        <rect x="470" y="250" width="160" height="64" rx="6" class="c-backend" stroke-width="1.5"/>
        <text x="550" y="280" class="t-primary" font-size="11" font-weight="600" text-anchor="middle">Spring API</text>
        <text x="550" y="296" class="t-muted" font-size="9" text-anchor="middle">Game endpoints</text>

        <rect x="710" y="250" width="160" height="64" rx="6" class="c-mask"/>
        <rect x="710" y="250" width="160" height="64" rx="6" class="c-backend" stroke-width="1.5"/>
        <text x="790" y="280" class="t-primary" font-size="11" font-weight="600" text-anchor="middle">Game Engine</text>
        <text x="790" y="296" class="t-muted" font-size="9" text-anchor="middle">route + combat</text>

        <rect x="950" y="250" width="160" height="64" rx="6" class="c-mask"/>
        <rect x="950" y="250" width="160" height="64" rx="6" class="c-database" stroke-width="1.5"/>
        <text x="1030" y="280" class="t-primary" font-size="11" font-weight="600" text-anchor="middle">MySQL</text>
        <text x="1030" y="296" class="t-muted" font-size="9" text-anchor="middle">runs + saves</text>

        <!-- Connection labels -->
        <rect x="180" y="262" width="30" height="14" rx="3" class="c-mask"/>
        <text x="195" y="272" class="t-backend" font-size="8" text-anchor="middle">play</text>
        <rect x="415" y="262" width="30" height="14" rx="3" class="c-mask"/>
        <text x="430" y="272" class="t-backend" font-size="8" text-anchor="middle">REST</text>
        <rect x="645.8" y="262" width="48.4" height="14" rx="3" class="c-mask"/>
        <text x="670" y="272" class="t-muted" font-size="8" text-anchor="middle">commands</text>
        <rect x="888.2" y="262" width="43.6" height="14" rx="3" class="c-mask"/>
        <text x="910" y="272" class="t-muted" font-size="8" text-anchor="middle">persist</text>

        <!-- Legend -->
        <text x="40" y="373" class="t-primary" font-size="9" font-weight="600">Legend</text>
        <rect x="40" y="378" width="14" height="9" rx="2" class="c-external" stroke-width="1"/>
        <text x="60" y="386" class="t-muted" font-size="8">External</text>
        <rect x="138" y="378" width="14" height="9" rx="2" class="c-frontend" stroke-width="1"/>
        <text x="158" y="386" class="t-muted" font-size="8">Frontend</text>
        <rect x="236" y="378" width="14" height="9" rx="2" class="c-backend" stroke-width="1"/>
        <text x="256" y="386" class="t-muted" font-size="8">Backend</text>
        <rect x="329" y="378" width="14" height="9" rx="2" class="c-database" stroke-width="1"/>
        <text x="349" y="386" class="t-muted" font-size="8">Database</text>
      </svg>
